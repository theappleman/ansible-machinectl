- name: start {{ container }} container
  machines: name={{ container }} ensure=running
  register: "{{ container }}_start"

- name: await container startup
  pause: seconds=10
  when: "{{ container }}_start|changed"
